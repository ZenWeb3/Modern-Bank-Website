import styles from "../style"

const Button = () => (
        <button type="button"
        className="py-4 px-6 bg-blue-gradient cursor-pointer w-[auto] font-semibold mt-10
         rounded-[10px] "
        >
            Get Started
        </button>
)

export default Button